---
import BaseLayout from "../layouts/BaseLayout.astro";
import BlogPost from "../components/blogpost/BlogPost.astro";
import TopPanel from "../components/topPanel/TopPanel";

import { getCollection } from "astro:content";
import { getUniqueTags } from "../utils/utils";

let blogPosts;

try {
  blogPosts = await getCollection("posts", (post) =>
    post.data.category?.match("history")
  );
} catch (err) {
  alert(err);
}
---

<BaseLayout title="History">
  <TopPanel client:load title="History" searchList={blogPosts} />
</BaseLayout>
