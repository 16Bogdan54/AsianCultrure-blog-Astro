---
import BaseLayout from "../layouts/BaseLayout.astro";
import BlogPost from "../components/blogpost/BlogPost.astro";
import TopPanel from "../components/topPanel/TopPanel";

import { getCollection } from "astro:content";
import { getUniqueTags } from "../utils/utils";

let blogPosts;

try {
  blogPosts = await getCollection("posts", (post) =>
    post.data.category?.match("cooking")
  );
  console.log(blogPosts[0].data);
} catch (err) {
  alert(err);
}
---

<BaseLayout title="Cooking">
  <TopPanel client:load title="Cooking" searchList={blogPosts} />
</BaseLayout>
